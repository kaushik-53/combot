/**
 * Toast — thin re-export of sonner's Toaster + a typed toast() helper.
 * coss.com/ui uses sonner as its toast primitive; we vendor this wrapper
 * so all call-sites import from '@/components/ui/toast'.
 */
export { Toaster } from 'sonner';
export { toast } from 'sonner';
